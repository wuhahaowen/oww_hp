import React,{useState,useRef} from 'react';
// ES6 模块（推荐）
// import  {Aether3d ,GLBLoaderScript  } from '../../utils/engine/aether3d-engine.es';
// import {EnvironmentMapScript} from '../../utils/engine/aether3d-engine.es';

function ThreeDViewer (){

    // const rendererRef = useRef<Aether3d | null>(null);
    // const environmentMapScriptRef = useRef<EnvironmentMapScript | null>(null);
    // const glbLoaderScriptRef = useRef<GLBLoaderScript | null>(null);


    return (
        <div className="threeDViewer">

        </div>
    )
};

export default ThreeDViewer;